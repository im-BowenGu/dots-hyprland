# This file is used when --via-nix is specified.
# Nix generates its own hypridle config via home-manager,
# so this file just prevents errors from missing file.
# The actual config is in sdata/dist-nix/home-manager/home.nix
